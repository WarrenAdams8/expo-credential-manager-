# Convex deployment base URL for HTTP actions
CONVEX_HTTP_URL=https://<your-deployment>.convex.site

# Optional: Override where the Expo app calls /api routes
# EXPO_PUBLIC_API_BASE_URL=http://<your-metro-host>:8081
